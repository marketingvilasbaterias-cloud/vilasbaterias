export default function Logo({ className = "h-16", variant = "default" }: { className?: string; variant?: "default" | "light" }) {
  return (
    <img
      src="/gemini_generated_image_jehfs5jehfs5jehf__1_-removebg-preview.png"
      alt="Vila's Baterias"
      className={className}
    />
  );
}
